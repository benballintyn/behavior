function [offset] = findOffset(data1,data2)
% data1 - ema_fast
% data2 - ema_slow
offset = 0;
for i=1:length(data1)
    if (data1(end-i) <= data2(end-i))
        break;
    else
        offset = offset+1;
    end
end
end

